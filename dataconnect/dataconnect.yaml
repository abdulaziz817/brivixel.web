specVersion: "v1beta"
serviceId: "Brixel"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Brixel-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
